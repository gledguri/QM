% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Package_functions.R
\name{combine_index}
\alias{combine_index}
\title{Combine multiple indices into a unique numeric index}
\usage{
combine_index(data, index_variables, combined_index_name)
}
\arguments{
\item{data}{A data.frame or tibble containing the indices to combine.}

\item{index_variables}{A character vector of column names representing the indices to combine.}

\item{combined_index_name}{A character string specifying the name of the new combined index column.}
}
\value{
A data.frame or tibble with an additional numeric index column representing the unique combinations.
}
\description{
This function converts multiple numeric index columns (that can be generated through `mutate_indices`) into a combined
numeric index by first converting each input index to a letter-based code (e.g., 1 -> "a", 27 -> "aa"),
combining these codes, and mapping the unique combinations to a numeric index.
}
\note{
The letter conversion uses modulo 26 arithmetic, cycling through letters "a"-"z".
Indices exceeding 26^3 (17,576) will produce duplicate codes. The combined index is ordered by the first unique combination encountered.
}
\examples{
data <- tibble(plate = c(1, 1, 2, 2), sample = c(1, 2, 1, 2))
combine_index(data, c("plate", "sample"), "unique_id") # Returns unique_id c(1, 2, 3, 4)
}
