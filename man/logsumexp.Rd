% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Package_functions.R
\name{logsumexp}
\alias{logsumexp}
\title{Log-Sum-Exponential (Numerically Stable)}
\usage{
logsumexp(x)
}
\arguments{
\item{x}{A numeric vector. For stability, input values should not be extremely large (see Note).}
}
\value{
A scalar value equal to \eqn{\log(\sum_{i=1}^n e^{x_i})}.
}
\description{
Computes the logarithm of the sum of exponentials of input values in a numerically stable way to prevent overflow.
}
\examples{
logsumexp(c(1, 2, 3)) # Returns log(exp(1) + exp(2) + exp(3)) ≈ 3.407606

# Avoids overflow compared to naive calculation:
x <- c(1000, 1001, 1002)
logsumexp(x) # Correctly returns ≈ 1002.408
log(sum(exp(x))) # Returns Inf due to overflow
}
